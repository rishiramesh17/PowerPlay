# Pinned to the versions this pipeline is known to run on. Unpinned installs of
# torch/ultralytics/opencv drift fast enough that detection results stop being
# reproducible between machines.

# --- API + job queue ---
fastapi==0.116.1
uvicorn==0.35.0
python-multipart==0.0.20

# --- video I/O ---
opencv-python==4.12.0.88
ffmpeg-python==0.2.0
yt-dlp==2026.7.4

# --- detection / OCR / re-ID ---
torch==2.8.0
torchvision==0.23.0
ultralytics==8.3.177
easyocr==1.7.2
numpy==2.5.1
pillow==12.3.0

# Only needed when DeepSORT tracking is enabled (PP_ENABLE_DEEPSORT /
# prefer_deepsort); processing/deepsort_track.py imports it unconditionally.
deep-sort-realtime==1.3.2

# --- optional VLM segment scoring ---
# Used by processing/highlight_scorer.py. Requires OPENAI_API_KEY; the pipeline
# runs without it, falling back to heuristic selection only.
openai==2.52.0
